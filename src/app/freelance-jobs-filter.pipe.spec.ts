import { FreelanceJobsFilterPipe } from './freelance-jobs-filter.pipe';

describe('FreelanceJobsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FreelanceJobsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
